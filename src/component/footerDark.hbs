<footer class="footer">
	{{#with footer}}
		<div class="footer_links">
			{{#each pages}}
				<a href="{{path}}" class="nav_link">{{name}}</a>
			{{/each}}
		</div>
		<div class="footer_copyright">
			<span>{{copyright}}</span>
		</div>
	{{/with}}
</footer>